class AppConfig {
  static const String apiBaseUrl = 'https://api.example.com';
  static const Duration connectTimeout =
      Duration(milliseconds: 5000); // milliseconds
  static const Duration receiveTimeout = Duration(milliseconds: 5000);
}
